import 'package:flutter/material.dart';

class ChatboxPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Chatbox'),
      ),
      body: Center(
        child: Text('Chatbox Page'),
      ),
    );
  }
}
